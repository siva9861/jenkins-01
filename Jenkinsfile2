pipeline {
    agent any

    stages {

         stage('Checkout') {
            steps {
                git url: "https://github.com/siva9861/jenkins-01.git", branch: 'java'
                sh "ls -ltr"
                
            }
        }  
        stage('Setup') {
            steps {
                

                sh "pip install -r requirements.txt"
            
            }
        }
        stage('Test') {
            steps {
                sh "pytest"
                
            }
        }    
        
        
            
    }
}